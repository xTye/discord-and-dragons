Working model for promise all.

await Promise.all([async () => {
    [...votes].forEach((array) => {
    if (array.numVotes > winner){
      tie = [];
      tie.push(array);
    }; 
    if (array.numVotes == winner)  
      tie.push(array);
  }
)}]);

Theme the:
Blue 1 Coastal
Blue 3 Deep ocean
Red 1 Meadow
Red 3 Volcano
Yellow 2 Cavern

Update the routes to go to green to blue

Picture of Dragon or Game Master
Are you going to go deeper in the ocean to get tickets.
